CREATE or REPLACE VIEW VENTAS_CLIENTE
AS
SELECT  v.ID_VENTA, v.ID_CLIENTE, v.ID_USUARIO, dv.CANTIDAD, p.PRODUCTO, c.NOMBRE
FROM VENTAS v, DVENTA dv , PRODUCTOS p, CLIENTES c
WHERE v.ID_VENTA = dv.ID_VENTA2 and dv.ID_PRODUCTO2 = p.ID_PRODUCTO and v.ID_CLIENTE = c.RUT